[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "amplify-portable-interface-lib"
version = "1.0.0"
authors = [
  { name="Thomas Röggla", email="t.roggla@cwi.nl" },
]
description = "Python interface library for AMPLIFY Portable"
requires-python = ">=3.8"
license = { text = "BSD 2-clause" }
dependencies = [
    "redis==7.0.1",
]
